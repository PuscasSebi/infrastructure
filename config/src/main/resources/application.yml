
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  application:
    name: config
  profiles:
    active: native #for classpath
  cloud:
    config:
      server:
        native:
          search-locations:
            - "classpath:/config"

server:
  port: 8071


management:
  health:
    probes:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'